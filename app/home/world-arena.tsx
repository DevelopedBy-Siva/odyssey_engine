import React from "react";
import { ScrollView, Text } from "react-native";

const WorldArena = () => {
  return (
    <ScrollView>
      <Text>WorldArena</Text>
    </ScrollView>
  );
};

export default WorldArena;
